= link_to '&larr; Назад в список причин'.html_safe, reject_reasons_path(kind: reject_reason.kind)
hr
h3 Новая причина отмены для #{t reject_reason.kind, scope: [:helpers, :kinds]}
hr
= render 'form', reject_reason: reject_reason
