h3 Новый пользователь
= simple_form_for operator, url: operators_path(operator) do |f|
  = f.input :email, as: :string, label: false, hint: 'Email/логин'
  = f.input :role, as: :select, collection: [['оператор', 'operator'], ['администратор', 'administrator']], label: false, hint: 'Роль', include_blank: false if operator.role_updatable_by?(current_user)
  = f.button :submit, 'Создать/изменить', class: 'btn btn-success'
