I"è(function() {
  var TIMEOUT, TYPES, i, len, type;

  TYPES = ['error', 'success', 'info', 'warning'];

  TIMEOUT = 5000;

  window.NotyFlash = {
    show: function(type, message) {
      return new Noty({
        text: message,
        timeout: TIMEOUT,
        type: type
      }).show();
    }
  };

  for (i = 0, len = TYPES.length; i < len; i++) {
    type = TYPES[i];
    window.NotyFlash[type] = function(message) {
      return this.show(type, message);
    };
  }

}).call(this);
:ET